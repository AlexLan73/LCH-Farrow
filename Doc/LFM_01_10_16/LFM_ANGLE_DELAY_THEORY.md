# 📡 Задержка с Шагом 0.5° и Гетеродин: Полное Руководство

## ЧАСТЬ 1: МАТЕМАТИКА И ТЕОРИЯ

---

## 1. ЗАДЕРЖКА С ШАГОМ 0.5° ПО УГЛУ

### 1.1 Базовые Параметры ЛЧМ

У тебя задано:
- **f_s = 12 МГц** — частота дискретизации (sampling rate)
- **f_0** — начальная частота ЛЧМ (нужно установить)
- **f_1** — конечная частота ЛЧМ (нужно установить)
- **T_duration** — длительность импульса ЛЧМ

### Стандартные значения для ЛЧМ радаров

Для типичного **радарного ЛЧМ** с fs = 12 МГц рекомендуются:

```
Вариант 1 (низкочастотный):
  f_0 = 2.4 ГГц (начало)
  f_1 = 2.5 ГГц (конец)
  BW = 100 МГц (полоса)

Вариант 2 (среднечастотный):
  f_0 = 5.0 ГГц
  f_1 = 5.1 ГГц
  BW = 100 МГц

Вариант 3 (для беспроводной антенны, более доступный):
  f_0 = 77 ГГц (автомобильный радар)
  f_1 = 77.5 ГГц
  BW = 500 МГц
```

**Рекомендация**: используй **Вариант 1** (2.4–2.5 ГГц) — стандарт для многих приложений.

### 1.2 Геометрия Антенной Решётки

**Линейная решётка из N элементов:**

```
[Элемент 0]  [Элемент 1]  [Элемент 2]  ...  [Элемент N-1]
     |             |             |                 |
     0         d·sin(θ)    2·d·sin(θ)       (N-1)·d·sin(θ)
     ├─────────────────────────────────────────────────────┤
                         Путь волны под углом θ
```

**Обозначения:**
- `d` = расстояние между элементами
- `θ` = угол прихода волны (направление от нормали)
- `k` = индекс элемента (0, 1, 2, ..., N-1)
- `λ` = длина волны
- `c` = скорость света = 3×10⁸ м/с

### 1.3 Формула Задержки по Углу

#### Длина волны (для центральной частоты)

$$\lambda = \frac{c}{f_c}, \quad \text{где} \quad f_c = \frac{f_0 + f_1}{2}$$

#### Расстояние между элементами (стандартно λ/2)

$$d = \frac{\lambda}{2} = \frac{c}{2 f_c}$$

#### Геометрическая задержка для элемента k при угле θ

Разность хода волны для элемента k:

$$\Delta L_k(\theta) = k \cdot d \cdot \sin(\theta)$$

Задержка по времени:

$$\tau_k(\theta) = \frac{k \cdot d \cdot \sin(\theta)}{c} = \frac{k \cdot d \cdot \sin(\theta)}{c}$$

#### Задержка в отсчётах (дискретном времени)

$$\text{delay\_samples}_k(\theta) = \tau_k(\theta) \cdot f_s = \frac{k \cdot d \cdot \sin(\theta)}{c} \cdot f_s$$

**Подставляя d = c/(2f_c):**

$$\text{delay\_samples}_k(\theta) = \frac{k \cdot \sin(\theta) \cdot f_s}{2 f_c}$$

### 1.4 Дискретизация по Углу с Шагом 0.5°

**Угол для луча i (в градусах):**

$$\theta_i = \theta_0 + i \cdot \Delta\theta, \quad \text{где} \quad \Delta\theta = 0.5^\circ$$

**В радианах:**

$$\theta_i = \theta_0 + i \cdot \frac{\pi}{360} \quad \text{(перевод 0.5° в радианы)}$$

**Пример диапазона:**
- θ_0 = -60°, θ_1 = -59.5°, θ_2 = -59°, ..., θ_240 = 60°
- Всего 241 луч (от -60° до +60° с шагом 0.5°)

### 1.5 Алгоритм: Вычисление Задержки на GPU

На GPU (OpenCL) для каждого луча i:

```
1. Вычислить угол: θ_i = θ_min + i * Δθ
2. Для каждого элемента k (0 до N-1):
   a. Вычислить sin(θ_i)
   b. Вычислить delay_k = k * sin(θ_i) * f_s / (2 * f_c)
   c. Выделить целую и дробную части
   d. Применить интерполяцию Лагранжа (как в твоём kernel!)
3. Сложить сигналы со всех элементов (beamforming)
```

---

## ЧАСТЬ 2: КОМПЛЕКСНОЕ СОПРЯЖЕНИЕ И ГЕТЕРОДИН

### 2.1 Теория Гетеродина

**Задача:** имеется полученный сигнал `x[n]` и опорный ЛЧМ `s[n]`. 
Нужно получить **baseband signal** (сигнал в полосе нулевой частоты).

**Математика:**

Если `s[n] = exp(j·φ[n])`, то сопряжённый сигнал:

$$s^*[n] = \exp(-j \cdot \varphi[n])$$

Произведение (heterodyning):

$$y[n] = x[n] \cdot s^*[n] = x[n] \cdot \exp(-j \cdot \varphi[n])$$

Результат: **спектр сдвинулся на частоту φ[n]**. Если x[n] содержал ЛЧМ такой же частоты, то y[n] станет **chirp-compressed signal** (сжатый импульс в частотной области).

### 2.2 Реализация: Комплексное Сопряжение

В C++:

```cpp
std::complex<float> z(3.0f, 4.0f);  // z = 3 + 4j
std::complex<float> z_conj = std::conj(z);  // z* = 3 - 4j
```

**Вручную (если нужно):**

```cpp
// Вход: complex<float> signal = Re + j*Im
// Выход: complex<float> conjugate = Re - j*Im
std::complex<float> signal(real_part, imag_part);
std::complex<float> conj_signal(signal.real(), -signal.imag());
```

### 2.3 Полный Процесс Гетеродина

```
Шаг 1: Генерируем опорный ЛЧМ
       s[n] = exp(j * φ_LFM[n])

Шаг 2: Получаем сопряжённый вариант
       s*[n] = exp(-j * φ_LFM[n])

Шаг 3: Перемножаем с приёмным сигналом
       y[n] = x[n] * s*[n]

Шаг 4: Результат — комплексный сигнал в baseband
       Если x[n] содержал такой же ЛЧМ:
       • Фаза отменяется (φ_in - φ_ref = 0)
       • Остаётся только информация о дальности и Доплере
       • Спектр сжимается (Pulse Compression)
```

---

## ЧАСТЬ 3: СТАНДАРТНЫЕ ЗНАЧЕНИЯ f_0 И f_1

### Таблица Радарных Стандартов

| Приложение | f_0 | f_1 | BW | Стандарт |
|-----------|-----|-----|-------|----------|
| **WiFi (2.4 ГГц)** | 2.4 ГГц | 2.5 ГГц | 100 МГц | 802.11 |
| **Автомобильный радар** | 77 ГГц | 77.5 ГГц | 500 МГц | IEEE 802.11p |
| **Морской радар** | 3 ГГц | 3.2 ГГц | 200 МГц | Морской стандарт |
| **Метеорологический** | 5.5 ГГц | 5.7 ГГц | 200 МГц | WMO |
| **Лабораторный** | 10 ГГц | 10.5 ГГц | 500 МГц | Универсальный |

### Рекомендация для твоего проекта

**Используй это (экспериментальный стандарт, близко к реальности):**

```cpp
float f_0 = 2.4e9f;    // 2.4 ГГц
float f_1 = 2.5e9f;    // 2.5 ГГц
float sample_rate = 12e6f;  // 12 МГц (дано)
```

**Проверка теоремы Найквиста:**
- Максимальная частота: 2.5 ГГц
- Минимальная f_s для Найквиста: 2 × 2.5 ГГц = 5 ГГц
- Твоя f_s = 12 МГц = 12×10⁶ Гц (!)

**⚠️ ПРОБЛЕМА:** f_s = 12 МГц очень мала для 2.4 ГГц сигнала! 

Скорее всего, в твоём случае:
- **Либо** это базовая полоса (baseband), тогда f_0/f_1 должны быть в МГц
- **Либо** используется undersampling (прямое преобразование высокой частоты)

**Альтернатива (для f_s = 12 МГц):**

```cpp
// Если это базовая полоса или промежуточная частота:
float f_0 = 1.0e6f;    // 1 МГц
float f_1 = 2.0e6f;    // 2 МГц
float sample_rate = 12e6f;  // 12 МГц ✓ (Найквист выполнен)
```

---

## ЧАСТЬ 4: ФОРМУЛЫ ВКРАТЦЕ

### Список основных формул

| № | Формула | Описание |
|---|---------|---------|
| 1 | λ = c / f_c | Длина волны (f_c — центральная частота) |
| 2 | d = λ / 2 | Расстояние между элементами |
| 3 | θ_i = θ_0 + i × 0.5° | Угол луча i (шаг 0.5°) |
| 4 | Δτ_k = (k·d·sin(θ)) / c | Геометрическая задержка элемента k |
| 5 | delay_k = Δτ_k × f_s | Задержка в отсчётах |
| 6 | φ(t) = 2π(f_0·t + (Δf/2T)·t²) | Фаза ЛЧМ (Δf = f_1 - f_0) |
| 7 | s[n] = exp(j·φ[n]) | Комплексный ЛЧМ сигнал |
| 8 | s*[n] = exp(-j·φ[n]) | Сопряжённый сигнал (гетеродин) |
| 9 | y[n] = x[n] × s*[n] | Гетеродинирование (умножение) |

---

**Версия:** 1.0  
**Статус:** Полная теория с формулами
