# 🔍 ОТЛАДКА ДРОБНОЙ ЗАДЕРЖКИ - Пошаговый маршрут

## 📍 Полный путь выполнения дробной задержки

### ШАГ 1: Точка входа - ProcessingPipeline
**Файл**: `src/processing_pipeline.cpp`  
**Строки**: 50-62

```50:62:src/processing_pipeline.cpp
    // 4. Дробная задержка
    profiler_->StartTimer("FractionalDelay");
    // TODO: Получить коэффициенты задержки (пока используем нули)
    std::vector<float> delay_coeffs(signal_buffer_->GetNumBeams(), 0.0f);
    if (!gpu_backend_->ExecuteFractionalDelay(
            device_buffer_,
            delay_coeffs.data(),
            signal_buffer_->GetNumBeams(),
            signal_buffer_->GetNumSamples())) {
        profiler_->StopTimer("FractionalDelay");
        return false;
    }
    profiler_->StopTimer("FractionalDelay");
```

**Что происходит**:
- Создаётся вектор коэффициентов задержки (пока все нули)
- Вызывается `ExecuteFractionalDelay` на GPU backend
- `device_buffer_` - указатель на буфер на GPU с данными сигналов

---

### ШАГ 2: Интерфейс IGPUBackend
**Файл**: `include/gpu_backend/igpu_backend.h`  
**Строки**: 70-77

```70:77:include/gpu_backend/igpu_backend.h
    virtual bool ExecuteFractionalDelay(
        void* device_buffer,
        const float* delay_coefficients,
        size_t num_beams,
        size_t num_samples
    ) = 0;
```

**Что происходит**:
- Виртуальный метод интерфейса
- Определяет сигнатуру функции

---

### ШАГ 3: Реализация OpenCLBackend
**Файл**: `src/gpu_backend/opencl_backend.cpp`  
**Строки**: 159-210

```159:210:src/gpu_backend/opencl_backend.cpp
bool OpenCLBackend::ExecuteFractionalDelay(
    void* device_buffer,
    const float* delay_coefficients,
    size_t num_beams,
    size_t num_samples) {
    
    if (!initialized_ || device_buffer == nullptr || delay_coefficients == nullptr) {
        return false;
    }
    
    try {
        cl::Buffer* buffer = static_cast<cl::Buffer*>(device_buffer);
        
        // Создаём буфер для коэффициентов задержки
        cl::Buffer delay_buf(
            context_,
            CL_MEM_READ_ONLY | CL_MEM_COPY_HOST_PTR,
            num_beams * sizeof(float),
            const_cast<float*>(delay_coefficients)
        );
        
        // Устанавливаем аргументы kernel
        cl_int err = kernel_fractional_delay_.setArg(0, *buffer);
        err |= kernel_fractional_delay_.setArg(1, delay_buf);
        err |= kernel_fractional_delay_.setArg(2, static_cast<cl_uint>(num_beams));
        err |= kernel_fractional_delay_.setArg(3, static_cast<cl_uint>(num_samples));
        
        if (!CheckError(err, "установка аргументов fractional_delay")) {
            return false;
        }
        
        // Запускаем kernel
        size_t global_size = num_beams * num_samples;
        err = queue_.enqueueNDRangeKernel(
            kernel_fractional_delay_,
            cl::NullRange,
            cl::NDRange(global_size),
            cl::NullRange
        );
        
        if (!CheckError(err, "запуск kernel fractional_delay")) {
            return false;
        }
        
        // Синхронизируем
        queue_.finish();
        return true;
    } catch (cl::Error& e) {
        std::cerr << "Ошибка при выполнении fractional_delay: " << e.what() 
                  << " (код: " << e.err() << ")" << std::endl;
        return false;
    }
}
```

**Что происходит**:
1. **Проверка валидности** (строка 165) - проверяет инициализацию и указатели
2. **Создание буфера коэффициентов** (строки 173-178) - создаёт OpenCL буфер для коэффициентов задержки
3. **Установка аргументов kernel** (строки 181-184):
   - Аргумент 0: `*buffer` - буфер с сигналами (in-place)
   - Аргумент 1: `delay_buf` - коэффициенты задержки
   - Аргумент 2: `num_beams` - количество лучей
   - Аргумент 3: `num_samples` - количество отсчётов
4. **Запуск kernel** (строки 191-197) - запускает kernel на GPU
5. **Синхронизация** (строка 203) - ждёт завершения выполнения

---

### ШАГ 4: OpenCL Kernel (GPU код)
**Файл**: `kernels/kernel_fractional_delay.cl`  
**Строки**: 18-69

```18:69:kernels/kernel_fractional_delay.cl
__kernel void fractional_delay(
    __global float2* input_output,
    __global const float* delay_coefficients,
    const uint num_beams,
    const uint num_samples
) {
    uint global_id = get_global_id(0);
    uint total_items = num_beams * num_samples;
    
    if (global_id >= total_items) {
        return;
    }
    
    // Определяем луч и отсчёт
    uint beam_id = global_id / num_samples;
    uint sample_id = global_id % num_samples;
    
    // Получаем коэффициент задержки для этого луча
    float delay = delay_coefficients[beam_id];
    
    // Вычисляем задержанный индекс (с дробной частью)
    float delayed_index = (float)sample_id - delay;
    
    // Простая линейная интерполяция
    uint idx_low = (uint)floor(delayed_index);
    uint idx_high = idx_low + 1;
    float t = delayed_index - (float)idx_low;
    
    // Обработка граничных условий
    if (idx_low >= num_samples) {
        idx_low = num_samples - 1;
    }
    if (idx_high >= num_samples) {
        idx_high = num_samples - 1;
    }
    
    // Индексы в глобальном буфере
    uint idx_current = beam_id * num_samples + sample_id;
    uint idx_delayed_low = beam_id * num_samples + idx_low;
    uint idx_delayed_high = beam_id * num_samples + idx_high;
    
    // Линейная интерполяция комплексных чисел
    float2 sample_low = input_output[idx_delayed_low];
    float2 sample_high = input_output[idx_delayed_high];
    
    float2 result;
    result.x = mix(sample_low.x, sample_high.x, t);  // real
    result.y = mix(sample_low.y, sample_high.y, t);  // imag
    
    // Записываем результат in-place
    input_output[idx_current] = result;
}
```

**Что происходит** (для каждого work item на GPU):
1. **Получение ID потока** (строка ~37) - `global_id` - уникальный ID потока
2. **Проверка границ** (строки ~40-42) - выход если ID вне диапазона
3. **Определение луча и отсчёта** (строки ~45-46):
   - `beam_id = global_id / num_samples` - номер луча
   - `sample_id = global_id % num_samples` - номер отсчёта
4. **Получение параметров задержки** (строки ~48-50):
   - `delay_integer` - целая часть задержки
   - `lagrange_row` - индекс строки матрицы Лагранжа [0, 47]
5. **Вычисление индекса для интерполяции** (строка ~53):
   - `interp_idx = sample_id - delay_integer - 2`
   - Используем 5 точек: [n-2, n-1, n, n+1, n+2]
6. **Интерполяция Лагранжа 5-го порядка** (строки ~59-84):
   - Цикл по 5 точкам (unroll для оптимизации)
   - Для каждой точки:
     - Вычисляем индекс с обработкой границ (отражение)
     - Берём отсчёт из входного буфера
     - Получаем коэффициент из матрицы Лагранжа
     - Применяем коэффициент к комплексному числу
7. **Запись результата** (строка ~88) - in-place запись в output буфер

**Алгоритм интерполяции Лагранжа**:
```
result = Σ(i=0 to 4) L_i(t) * input[sample_id - delay_integer - 2 + i]

где:
  L_i(t) - коэффициент из матрицы Лагранжа [lagrange_row][i]
  t - дробная часть задержки (определяет lagrange_row)
  5 точек: [-2, -1, 0, +1, +2] относительно задержанного индекса
```

---

## 🎯 Точки останова (Breakpoints) для отладки

### Рекомендуемые точки останова:

1. **`src/main.cpp:~25`** - Загрузка матрицы Лагранжа
   - Проверить что матрица загружена из JSON
   - Проверить `lagrange_matrix.IsValid()`

2. **`src/main.cpp:~40`** - Загрузка матрицы на GPU
   - Проверить что `UploadLagrangeMatrix()` успешно

3. **`src/processing_pipeline.cpp:54`** - Перед вызовом ExecuteFractionalDelay
   - Проверить `delay_coeffs` - коэффициенты задержки
   - Проверить `device_buffer_` - указатель на GPU буфер

4. **`src/gpu_backend/opencl_backend.cpp:165`** - Начало ExecuteFractionalDelay
   - Проверить валидность параметров
   - Проверить `delay_coefficients` - массив коэффициентов
   - Проверить `lagrange_matrix_uploaded_` - должна быть true

5. **`src/gpu_backend/opencl_backend.cpp:~175`** - Вычисление параметров задержки
   - Проверить `delay_params[beam].delay_integer` - целая часть
   - Проверить `delay_params[beam].lagrange_row` - индекс строки [0, 47]

6. **`src/gpu_backend/opencl_backend.cpp:~200`** - Перед установкой аргументов
   - Проверить `buffer` - указатель на OpenCL буфер
   - Проверить `lagrange_matrix_buffer_` - буфер матрицы
   - Проверить `delay_params_buf` - буфер параметров

7. **`src/gpu_backend/opencl_backend.cpp:~210`** - Перед запуском kernel
   - Проверить `global_size` - общее количество work items
   - Проверить `work_group_size` - размер work group

8. **`src/gpu_backend/opencl_backend.cpp:~220`** - После синхронизации
   - Проверить результат выполнения
   - Проверить ошибки OpenCL

---

## 📊 Структура данных

### На хосте (CPU):
- `lagrange_matrix` - матрица `float[48][5]` - коэффициенты Лагранжа
- `delay_coefficients` - массив `float[num_beams]` - коэффициенты задержки для каждого луча
- `device_buffer_` - указатель на буфер на GPU

### На устройстве (GPU):
- `input` - буфер `float2[num_beams * num_samples]`:
  - Каждый элемент - комплексное число (real, imag)
  - Структура: `[луч0_отсчёт0, луч0_отсчёт1, ..., луч0_отсчётN, луч1_отсчёт0, ...]`
- `output` - буфер `float2[num_beams * num_samples]` (in-place, может быть = input)
- `lagrange_matrix` - буфер `float[48 * 5]` - матрица коэффициентов Лагранжа (constant memory)
- `delay_params` - буфер `DelayParams[num_beams]`:
  ```c
  struct DelayParams {
      int delay_integer;    // Целая часть задержки
      int lagrange_row;     // Индекс строки матрицы [0, 47]
  };
  ```

---

## 🔧 Алгоритм дробной задержки с интерполяцией Лагранжа

### Математика:
Для каждого отсчёта `sample_id` в луче `beam_id`:

1. **Разделение задержки**:
   - `delay_integer = floor(delay[beam_id])` - целая часть
   - `delay_fraction = delay[beam_id] - delay_integer` - дробная часть [0, 1)

2. **Выбор строки матрицы Лагранжа**:
   - `lagrange_row = (int)(delay_fraction * 48)` - индекс строки [0, 47]

3. **Интерполяция Лагранжа 5-го порядка**:
   - Используем 5 точек: `[n-2, n-1, n, n+1, n+2]`
   - `interp_idx = sample_id - delay_integer - 2`
   - `result = Σ(i=0 to 4) L_i * input[interp_idx + i]`
   - где `L_i = lagrange_matrix[lagrange_row][i]`

### Пример:
Если `delay = 5.375` и `sample_id = 100`:
- `delay_integer = 5`, `delay_fraction = 0.375`
- `lagrange_row = (int)(0.375 * 48) = 18`
- `interp_idx = 100 - 5 - 2 = 93`
- Используем точки: `input[93], input[94], input[95], input[96], input[97]`
- Коэффициенты: `L[18][0], L[18][1], L[18][2], L[18][3], L[18][4]`
- `result = L[18][0]*input[93] + L[18][1]*input[94] + ... + L[18][4]*input[97]`

---

## 🐛 Типичные проблемы для отладки

1. **Матрица Лагранжа не загружена**:
   - Проверить что `lagrange_matrix_uploaded_ == true`
   - Проверить путь к JSON файлу
   - Проверить что `UploadLagrangeMatrix()` вызван

2. **Нулевые коэффициенты задержки**:
   - В `processing_pipeline.cpp:53` сейчас все коэффициенты = 0.0
   - Это означает отсутствие задержки (сигнал не изменяется)
   - Для тестирования задать ненулевые значения

3. **Неправильный индекс строки матрицы**:
   - `lagrange_row` должен быть в [0, 47]
   - Проверить вычисление: `lagrange_row = (int)(delay_fraction * 48)`

4. **Выход за границы массива**:
   - Проверить обработку границ в kernel (отражение)
   - Убедиться что `interp_idx + i` обрабатывается корректно

5. **Неправильная индексация**:
   - Формула: `idx = beam_id * num_samples + sample_id`
   - Проверить что данные правильно упакованы

6. **Проблемы с синхронизацией**:
   - Убедиться что `queue_.finish()` вызывается после kernel
   - Проверить что данные скопированы на GPU перед вызовом

7. **Ошибка компиляции kernel**:
   - Проверить что структура `DelayParams` правильно определена
   - Проверить что все аргументы kernel установлены

---

## 📝 Заметки для отладки

- **Kernel выполняется параллельно**: Каждый work item обрабатывает один отсчёт одного луча
- **In-place обработка**: Результат записывается в output буфер (может быть = input)
- **Комплексные числа**: Используется `float2` (x=real, y=imag)
- **Граничные условия**: Отражение от начала/конца массива
- **Интерполяция Лагранжа**: 5-й порядок, точность выше чем линейная интерполяция
- **Матрица Лагранжа**: 48 строк × 5 столбцов, загружается один раз при инициализации
- **Разрешение задержки**: 1/48 шага дискретизации (2.083%)

## 📁 Файлы для отладки

1. **Матрица Лагранжа**: `Doc/Example/lagrange_matrix.json`
2. **Kernel**: `kernels/kernel_fractional_delay.cl`
3. **Загрузка матрицы**: `src/lagrange_matrix.cpp`
4. **Backend**: `src/gpu_backend/opencl_backend.cpp` (строки 159-230)
5. **Pipeline**: `src/processing_pipeline.cpp` (строки 50-62)

---

*Создано: 2025-01-28*  
*Для отладки дробной задержки сигнала*

