# Farrow Filter для ЛЧМ Сигналов - Матрица Коэффициентов 48×5

## 📋 ФОРМАТ ФАЙЛА

**Имя файла:** `farrow_coeffs_48x5.txt`

**Первая строка:** `48 5`  
(48 строк = длина фильтра L, 5 столбцов = порядок полинома P+1)

**Далее 48 строк по 5 чисел:**

```
c[0][0]  c[0][1]  c[0][2]  c[0][3]  c[0][4]
c[1][0]  c[1][1]  c[1][2]  c[1][3]  c[1][4]
...
c[47][0] c[47][1] c[47][2] c[47][3] c[47][4]
```

## 🔬 ФИЗИЧЕСКИЙ СМЫСЛ

| Строка p | Значение |
|----------|----------|
| 0 | Базовая интерполяция |
| 1 | Первая производная |
| 2 | Вторая производная |
| 3 | Третья производная |
| 4 | Четвёртая производная |

## ✅ ПРИМЕР ВАЛИДНОГО ФАЙЛА

```
48 5
1.000000  0.000000  0.000000  0.000000  0.000000
0.950000  0.050000  0.000000  0.000000  0.000000
0.925000  0.075000  0.000001  0.000000  0.000000
...
```

## 📊 ПРОВЕРКА КОЭФФИЦИЕНТОВ

Сумма первой строки (интерполяция) должна быть ≈ 1.0:
```
sum(c[0]) = 1.0 ± 0.001
```

Суммы остальных строк должны быть ≈ 0.0:
```
sum(c[p>0]) = 0.0 ± 0.001
```

## 💡 БЫСТРОЕ СОЗДАНИЕ (Python)

```python
import numpy as np

# Создать sample матрицу (Lagrange интерполяция)
L, P = 48, 4
coeffs = np.zeros((P+1, L))

# Заполнить вашими значениями
# (или использовать из файла sample)

# Сохранить
with open('farrow_coeffs_48x5.txt', 'w') as f:
    f.write(f"{L} {P+1}\n")
    for row in coeffs:
        f.write(' '.join(f"{x:.6f}" for x in row) + '\n')
```

## 🚀 КАК ИСПОЛЬЗОВАТЬ

1. Создайте файл `farrow_coeffs_48x5.txt`
2. Запустите программу: `./farrow_chirp_test`
3. Программа автоматически загрузит коэффициенты
