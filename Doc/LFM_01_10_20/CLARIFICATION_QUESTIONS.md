# 📋 CLARIFICATION QUESTIONS - FAQ

## Q1: Зачем нужны параметризованные углы?

**A:** Раньше были жёсткие ±60° и 241 луч. Теперь можно указать любой диапазон и шаг, чтобы тестировать на малом наборе (5-8 лучей) перед полным расчётом.

```cpp
// Вместо жёстких ±60°, 241 луч:
params.angle_start_deg = -5.0f;   // Только ±5°
params.angle_stop_deg = 5.0f;
params.angle_step_deg = 1.0f;     // Шаг 1°
// → 11 лучей вместо 241
```

---

## Q2: Почему размер должен быть 2^n?

**A:** Для оптимизации GPU FFT (быстрое преобразование Фурье). Размеры 512, 1024, 2048 вычисляются намного быстрее, чем произвольные размеры.

---

## Q3: Duration вычисляется автоматически?

**A:** ДА! Формула: `duration = num_samples / sample_rate`

```cpp
// Ты задаёшь:
params.num_samples = 512;
params.sample_rate = 12.0e6f;

// Вычисляется автоматически:
duration = 512 / 12e6 = 42.67 µs
```

Не нужно задавать вручную.

---

## Q4: Где сохраняются задержанные сигналы?

**A:** В приватной переменной `m_signal_conjugate[angle_idx]`, которая содержит для каждого угла:
- ЛЧМ сигнал
- + геометрическая задержка (Лагранжа)
- + сопряжение conj()

JSON экспортирует это в метаданные + массив reference_signals.

---

## Q5: Как работает Лагранжева интерполяция?

**A:** Для дробной задержки (например, 10.75 отсчётов) используется интерполяция Лагранжа порядка 48 с центром в позиции 5.

```cpp
delay_samples = 10.75
delay_int = 10           // целая часть
delay_frac = 0.75        // дробная часть

// Лагранжа интерполирует между samples 5-9 и 10-14
result = Σ L_i(0.75) * signal[10-5+i]
```

Точность: ±1e-6 (как на GPU).

---

## Q6: Что означает "порядок 48, позиция 5"?

**A:**
- **Порядок 48**: используются 48 соседних точек для интерполяции
- **Позиция 5**: центр окна смещен на 5 позиций влево от целой части задержки

Это совпадает с GPU реализацией.

---

## Q7: Зачем сопряжение (conj)?

**A:** Для гетеродина на GPU:
```
y[n] = x[n] × conj(ref[n])
```

Сопряжение переносит сигнал в baseband (около 0 МГц) для дальнейшей обработки.

---

## Q8: JSON слишком большой?

**A:** Размер JSON ≈ 750 КБ для конфига по умолчанию.

Формула: `num_angles × num_samples × 24 байт`

Это нормально. Можно сжать gzip для передачи на GPU.

---

## Q9: Как интегрировать с GPU?

**A:**
1. CPU генерирует и сохраняет JSON (этот проект)
2. GPU загружает JSON
3. GPU читает приёмный сигнал x[n]
4. GPU вычисляет: `y[n] = x[n] * conj(ref[n])` для каждого луча
5. GPU запускает FFT

```c
// GPU kernel (псевдокод):
for (each beam) {
    for (n = 0 to num_samples) {
        float2 x = rx_signal[n];
        float2 ref_conj = conj(reference_signals[beam][n]);
        y[n] = complex_multiply(x, ref_conj);
    }
    fft(y);
}
```

---

## Q10: Пример конфига для быстрого теста?

**A:**
```cpp
// Быстрый тест: -5..+5°, шаг 1°, 11 лучей
params.angle_start_deg = -5.0f;
params.angle_stop_deg = 5.0f;
params.angle_step_deg = 1.0f;
params.num_samples = 256;  // 2^8
```

Займёт <10 мс на CPU.

---

## Q11: Что если задержка выходит за границы?

**A:** Результат считается 0 (нет сигнала). Обычно задержка в пределах одного периода, так что этого не происходит.

---

## Q12: Как проверить, что всё работает?

**A:**
1. Запусти программу
2. Проверь JSON: `python -m json.tool reference_signals.json | head`
3. Запусти Python валидацию (см. QUICK_START.md)
4. Проверь FINAL_CHECKLIST.md

---

## Q13: Компилятор старый, нет C++17?

**A:** Минимум нужен C++17 для `std::complex` и других фич. Обновите компилятор или используйте современный.

```bash
g++ --version
# должно быть >= 7.0
```

---

## Q14: Как менять параметры Лагранжа?

**A:**
```cpp
// Порядок интерполяции
params.lagrange_order = 48;  // или 24, 64, 96...
// должно быть чётное число

// Позиция центра
params.lagrange_row = 5;     // или 3, 4, 6... < order/2
```

Оставить по умолчанию: это совпадает с GPU.

---

## Q15: Сколько памяти нужно?

**A:**
```
JSON: 61 × 512 × 24 ≈ 750 КБ
Runtime: 61 × 512 × 8 ≈ 250 КБ (сложные)
Всего: <1 МБ
```

Очень компактно.

---

## ✅ ГОТОВО!
