# 📄 ONE PAGE SUMMARY

## ✅ 5 РЕШЕНИЙ

| Проблема | Решение |
|----------|---------|
| Углы ±60° жёсткие | Любой диапазон параметризуемый |
| Размер неизвестен | Только 2^n (512, 1024, 2048...) |
| Duration неясен | Вычисляется автоматически |
| Где сохранить | m_signal_conjugate[] → JSON |
| Лагранжа как на GPU | Порядок 48, позиция 5 |

---

## 🚀 3 ШАГА

```cpp
// 1. Параметры
AngleArrayParams params;
params.f_start = 1.0e6f;
params.f_stop = 2.0e6f;
params.sample_rate = 12.0e6f;
params.num_samples = 512;
params.angle_start_deg = -15.0f;
params.angle_stop_deg = 15.0f;
params.angle_step_deg = 0.5f;

// 2. Генерируем
LFMAngleArray arr(params);
arr.Generate();

// 3. JSON
std::ofstream("ref.json") << arr.ExportToJSON();
```

---

## 📊 РЕЗУЛЬТАТ

- 61 угол (от -15° до +15°)
- 512 отсчётов каждый
- Каждый: ЛЧМ + задержка (Лагранжа) + conj()
- JSON для гетеродина на GPU

---

## 🔧 КЛЮЧЕВЫЕ ПАРАМЕТРЫ

- `angle_start_deg`, `angle_stop_deg`, `angle_step_deg` — диапазон и шаг
- `num_samples` — степень 2 (2^9=512, 2^10=1024...)
- `antenna_element_idx` — элемент массива
- `lagrange_order=48`, `lagrange_row=5` — как на GPU

---

## ✅ ГОТОВО!
